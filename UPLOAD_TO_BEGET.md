# 📤 Загрузка проекта на Beget через файловый менеджер

## Способ 1: Через файловый менеджер Beget (Рекомендуется)

### Шаг 1: Подготовка архива
Архив уже создан: `finance-app.tar.gz` (130 KB)

### Шаг 2: Загрузка через панель Beget

1. **Откройте панель Beget**: https://cp.beget.com
2. **Перейдите в ваш сервер** "Rejoicing Valeriy"
3. **Откройте "Файловый менеджер"** или "File Manager"
4. **Перейдите в директорию** `/root/`
5. **Загрузите архив** `finance-app.tar.gz`:
   - Нажмите "Загрузить" или "Upload"
   - Выберите файл `finance-app.tar.gz` с вашего компьютера
   - Дождитесь завершения загрузки

### Шаг 3: Распаковка на сервере

В веб-терминале Beget выполните:

```bash
cd /root
tar -xzf finance-app.tar.gz -C finance-app
cd finance-app
```

Или если директории нет:
```bash
cd /root
mkdir finance-app
tar -xzf finance-app.tar.gz -C finance-app
cd finance-app
```

### Шаг 4: Установка и запуск

```bash
# Установка Node.js (если не установлен)
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs

# Установка зависимостей
npm install

# Сборка приложения
npm run build

# Установка PM2
npm install -g pm2

# Запуск
pm2 start ecosystem.config.js
pm2 save
```

---

## Способ 2: Через SCP (из терминала Mac)

### Шаг 1: Загрузите архив на сервер

В терминале на вашем Mac выполните:

```bash
cd "/Users/gennadyzakharov/Documents/Новая папка/finance-app"
scp finance-app.tar.gz root@45.80.69.195:/root/
```

Введите пароль: `gjWztP~6!8GC`

### Шаг 2: Подключитесь к серверу и распакуйте

```bash
ssh root@45.80.69.195
cd /root
mkdir -p finance-app
tar -xzf finance-app.tar.gz -C finance-app
cd finance-app
npm install
npm run build
npm install -g pm2
pm2 start ecosystem.config.js
pm2 save
```

---

## Способ 3: Прямая загрузка папки через SFTP

### Используйте FileZilla или другой SFTP клиент:

1. **Хост**: `45.80.69.195`
2. **Пользователь**: `root`
3. **Пароль**: `gjWztP~6!8GC`
4. **Порт**: `22`
5. **Протокол**: SFTP

Загрузите всю папку `finance-app` в `/root/`

---

## Способ 4: Через Git (если Git установлен на сервере)

В веб-терминале Beget:

```bash
cd /root
git clone https://github.com/zaharrrov001-hue/finans.git finance-app
cd finance-app
npm install
npm run build
npm install -g pm2
pm2 start ecosystem.config.js
pm2 save
```

---

## ✅ Рекомендация

**Самый простой способ**: Используйте файловый менеджер Beget + веб-терминал для распаковки и установки.

