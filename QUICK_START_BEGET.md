# 🚀 Быстрый старт: Подключение к Beget

## Шаг 1: Откройте Терминал на Mac

**Способ 1:**
- Нажмите `Cmd + Space` (Spotlight)
- Введите "Терминал" или "Terminal"
- Нажмите Enter

**Способ 2:**
- Откройте Finder
- Программы → Утилиты → Терминал

## Шаг 2: Подключитесь к серверу

В терминале введите команду:

```bash
ssh root@45.80.69.195
```

**Что произойдет:**
1. Может появиться вопрос "Are you sure you want to continue connecting?" — введите `yes`
2. Запросит пароль — введите пароль root из панели Beget
3. После успешного подключения вы увидите приглашение: `[root@... ~]#`

## Шаг 3: После подключения выполните деплой

Скопируйте и выполните эти команды по очереди:

```bash
# Переходим в домашнюю директорию
cd /root

# Клонируем репозиторий
git clone https://github.com/zaharrrov001-hue/finans.git finance-app

# Переходим в директорию проекта
cd finance-app

# Делаем скрипт исполняемым
chmod +x deploy-beget.sh

# Запускаем автоматический деплой
./deploy-beget.sh
```

## Шаг 4: Дождитесь завершения

Скрипт автоматически:
- ✅ Установит Node.js
- ✅ Установит зависимости
- ✅ Соберет приложение
- ✅ Запустит через PM2

**Время выполнения:** 5-10 минут

## Шаг 5: Проверьте статус

После завершения выполните:

```bash
pm2 status
```

Должно показать, что приложение `finance-app` запущено.

## Шаг 6: Настройте домен (опционально)

В панели Beget настройте Nginx для проксирования на `localhost:3000`

---

## ❓ Проблемы?

### "Permission denied"
- Проверьте пароль root
- Убедитесь, что SSH ключ добавлен в панели Beget

### "Connection refused"
- Проверьте, что сервер запущен в панели Beget
- Проверьте IP адрес: `45.80.69.195`

### "Command not found: git"
- Установите git: `yum install -y git` или `apt-get install -y git`

---

## 📞 Нужна помощь?

Если что-то не работает, пришлите:
1. Текст ошибки из терминала
2. Скриншот панели Beget


