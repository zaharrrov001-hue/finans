# 🚀 Простая инструкция по деплою сайта

## Вариант 1: Vercel (САМЫЙ ПРОСТОЙ) ⭐ Рекомендуется

### Шаги:

1. **Откройте**: https://vercel.com
2. **Войдите через GitHub** (кнопка "Sign in with GitHub")
3. **Нажмите "Add New Project"**
4. **Выберите репозиторий**: `zaharrrov001-hue/finans`
5. **Нажмите "Deploy"**

**Всё!** Через 2-3 минуты сайт будет доступен по адресу типа: `https://finans-xxx.vercel.app`

**Преимущества:**
- ✅ Бесплатно
- ✅ Автоматический деплой при каждом push в GitHub
- ✅ SSL сертификат автоматически
- ✅ Не нужно настраивать сервер
- ✅ Работает из коробки

---

## Вариант 2: Beget (если нужен ваш домен)

### Упрощенная инструкция:

#### Шаг 1: Откройте веб-терминал Beget
- Зайдите в панель: https://cp.beget.com
- Откройте ваш сервер "Rejoicing Valeriy"
- Нажмите "Терминал" или "VNC"
- Войдите: `root` → пароль `2&UPny4fHa#P`

#### Шаг 2: Выполните ОДНУ команду

Скопируйте и вставьте эту команду целиком в терминал:

```bash
cd /root && git clone https://github.com/zaharrrov001-hue/finans.git finance-app && cd finance-app && curl -fsSL https://deb.nodesource.com/setup_20.x | bash - && apt-get install -y nodejs && npm install && npm run build && npm install -g pm2 && pm2 start ecosystem.config.js && pm2 save && pm2 status
```

**Всё!** Приложение запустится автоматически.

#### Шаг 3: Настройте домен (опционально)

В панели Beget настройте Nginx для проксирования на `localhost:3000`

---

## Какой вариант выбрать?

- **Vercel** - если нужен быстрый деплой без настройки
- **Beget** - если нужен ваш домен и больше контроля

**Рекомендую Vercel** - это займет 2 минуты вместо часов настройки!

